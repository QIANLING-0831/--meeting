# 自定义知识包

每种岗位建立一个子文件夹，再放入 `.md`、`.txt` 或 `.pdf` 文件。例如：

```text
packs/
├── java/
│   ├── Java八股.md
│   └── JVM.md
├── backend/
│   ├── MySQL.md
│   └── Redis.md
└── llm/
    ├── RAG.md
    └── Agent.md
```

启动页面后点击“重建索引”，子文件夹名称就会作为可勾选的知识包出现。
不要在资料中写入 API Key、密码或其他凭据。

